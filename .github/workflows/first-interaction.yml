name: First Interaction

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  first-interaction:
    runs-on: ubuntu-latest
    steps:
      - name: Greet first-time contributors
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 Hi @{{ github.actor }}!
            Thanks for opening your first issue in this repository 🎉
            A maintainer will look into it soon.

          pr-message: |
            🎉 Hi @{{ github.actor }}!
            Thanks for submitting your first Pull Request 🚀
            We really appreciate your contribution!
